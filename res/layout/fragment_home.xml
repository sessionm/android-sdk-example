<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/parent_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
    android:orientation="vertical" >

    <!-- The mPOINTS rewards button allows the user to access the mPOINTS
    user portal from within your app. In the user portal users can redeem
    their achievement for real rewards. As part of the integration you should
    put a button somewhere in the app which opens the portal. In the layout
    file for your activity, identify where you would like to place the button
    and add the following -->

    <com.sessionm.api.PortalButton
        android:id="@+id/portalButton"
        android:layout_width="85dp"
        android:layout_height="85dp"
        android:layout_alignParentRight="true"
        android:layout_margin="10dp"
        android:scaleType="fitStart" >
    </com.sessionm.api.PortalButton>

    <com.sessionm.api.PortalButton
        android:id="@+id/home_logo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" >
    </com.sessionm.api.PortalButton>

</RelativeLayout>